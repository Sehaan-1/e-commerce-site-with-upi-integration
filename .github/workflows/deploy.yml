name: Deploy

on:
  push:
    branches: [main, master]
    paths-ignore:
      - "**.md"
      - "docs/**"
      - ".github/workflows/ci.yml"

  # Allow manual deploys from the GitHub UI
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: "production"
        type: choice
        options:
          - production
          - staging

env:
  NODE_VERSION: "20"
  REGISTRY: ghcr.io
  # Replace with your GitHub username/org and repo name
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # ── Build & Push Docker Image ───────────────────────────────────────────
  docker:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write  # needed to push to GHCR

    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
      image-digest: ${{ steps.build.outputs.digest }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,format=short
            type=ref,event=branch
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' }}

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ── Deploy to Railway ───────────────────────────────────────────────────
  # Uncomment if deploying to Railway
  # deploy-railway:
  #   name: Deploy to Railway
  #   runs-on: ubuntu-latest
  #   needs: [docker]
  #   environment: production
  #   steps:
  #     - name: Install Railway CLI
  #       run: npm install -g @railway/cli
  #     - name: Deploy
  #       run: railway up --service kalpa-living
  #       env:
  #         RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

  # ── Deploy to Render ────────────────────────────────────────────────────
  # Uncomment if deploying to Render (uses deploy hook)
  # deploy-render:
  #   name: Deploy to Render
  #   runs-on: ubuntu-latest
  #   needs: [docker]
  #   environment: production
  #   steps:
  #     - name: Trigger Render deploy hook
  #       run: |
  #         curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"

  # ── Deploy to VPS via SSH ───────────────────────────────────────────────
  # Uncomment if deploying to a self-hosted VPS
  # deploy-vps:
  #   name: Deploy to VPS
  #   runs-on: ubuntu-latest
  #   needs: [docker]
  #   environment: production
  #   steps:
  #     - name: Deploy via SSH
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.VPS_HOST }}
  #         username: ${{ secrets.VPS_USER }}
  #         key: ${{ secrets.VPS_SSH_KEY }}
  #         script: |
  #           docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
  #           docker compose -f /opt/kalpa-living/docker-compose.yml up -d --remove-orphans
  #           docker image prune -f
